package com.rra.project.riotrestapi.service.datadragon.datatypes;

//needs to be restructured with backend
public record GameModeData(
        int key,
        String map,
        String modeName
) {}
